{
  "dependencies": {
    "conventional-changelog-cli": "^2.2.2"
  },
  "version": "1.3.0",
  "scripts": {
    "generate-version": "node assets/js/generate-version.js",
    "version": "conventional-changelog -i CHANGELOG.md -s && git add CHANGELOG.md && npm run generate-version && git add assets/js/version.js"
  }
}
