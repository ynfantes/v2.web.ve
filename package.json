{
  "dependencies": {
    "conventional-changelog-cli": "^2.2.2"
  },
  "version": "1.2.2",
  "scripts": {
    "generate-version": "node scripts/generate-version.js",
    "version": "conventional-changelog -i CHANGELOG.md -s && git add CHANGELOG.md && npm run generate-version"
  }
}
