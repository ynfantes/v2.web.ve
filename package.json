{
  "dependencies": {
    "conventional-changelog-cli": "^2.2.2"
  },
  "version": "1.3.6",
  "scripts": {
    "generate-version": "node assets/js/generate-version.js",
    "version": "conventional-changelog -i CHANGELOG.md -s && npm run generate-version && git add ."
  }
}
